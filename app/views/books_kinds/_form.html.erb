<%= form_for(@books_kind) do |f| %>
  <% if @books_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@books_kind.errors.count, "error") %> prohibited this books_kind from being saved:</h2>

      <ul>
      <% @books_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cote_books %><br>
    <%= f.text_field :cote_books %>
  </div>
  <div class="field">
    <%= f.label :cote_kind %><br>
    <%= f.text_field :cote_kind %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
